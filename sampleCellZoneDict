
sampleCellZone
{
	fields (U p);
	type mySets;
	libs ("libmycloud.so");
	writeControl timeStep;
	outputInterval 1;
	interpolationScheme cellPoint;
	setFormat csv;
	sets
	(
		data
		{
			type cloud;
			axis xyz;
			points (1 2 3 4);
			cellZone s2;
		}
	);
}
